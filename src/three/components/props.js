const canvasProps = { width: 1280, height: 720 };
const renderProps = { isRendering: false };
const imageProps = { loaded: false, file: null,format:'' }

export { canvasProps, renderProps, imageProps };